// import Tabs from '../components/main';

// test('initial display of tabs', () => {
//   expect(Tabs.to_s).toMatch(/content/);
// });

test('adds 1 + 2 to equal 3', () => {
  expect((1 + 2)).toBe(3);
});
